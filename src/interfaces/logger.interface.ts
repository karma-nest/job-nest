/**
 * @fileoverview
 * @version
 * @module loggerConfig
 */

interface ILoggerConfig {
  application: string;
  auth: string;
  candidate: string;
  job: string;
  recruiter: string;
  system: string;
}

export { ILoggerConfig };
