/**
 * @fileoverview
 * @version
 * @module loggerConfig
 */

interface ILoggerConfig {
  admin: string;
  application: string;
  auth: string;
  candidate: string;
  job: string;
  recruiter: string;
  system: string;
}

export { ILoggerConfig };
